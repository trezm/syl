{
  "name": "syl",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w packages/core && npm run build -w packages/channel && concurrently -n server,web -c blue,green \"PORT=${SYL_SERVER_PORT:-3000} npm run dev -w packages/server\" \"npm run dev -w packages/web\"",
    "build": "npm run build -w packages/core && npm run build -w packages/channel && npm run build -w packages/server && npm run build -w packages/web",
    "grammars:build": "node scripts/build-grammars.mjs",
    "grammars:check": "node scripts/build-grammars.mjs --check"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "tree-sitter-cli": "0.26.12",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
